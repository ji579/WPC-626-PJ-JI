**
상단 사이트 아이콘 사이즈변경사이트
https://favicon.io/


자바스크립트로 
JS8-2.DOM_이벤트객체_ a 눌렀을때 위로 안올라가게 막는거 참고하기 67글
  // 기본기능막기(a요소는 이동특성이 있음. 이것을 막음!)
  // e.preventDefault();
  return false;

////////////////////
css 09-2nth child 참고
label태그?

패딩이 없어야 닫힘 만약 디자인이 필요하면 패딩박스가 따로있어야함

블릿 참고
main3js
03페이드.html

// 휠 이벤트 막기 테스트 
휠로 내리면서 화면전환또는 옆에서 나오고 할때 필요

//// 220줄
window.addEventListener(
  "wheel",
  (e) => {
    // 기본기능막기
    e.preventDefault();

    console.log("휠~~~~");
    // 배경색 변경
    myFn.qs("body").style.backgroundColor = "lightcoral";
  },
  { passive: false }
);
// 이벤트막기할 경우 window, body, document
// 일 경우 막기를 할 수 없게 셋팅됨!
// 이것을 쓰려면 이벤트 설정시 맨뒤에 {passive:false}
// 설정을 추가한다!
    //////////////////////////////////////////

user-select: none;
    -webkit-user-drag: none;

***인트로 영상 넘어가기 코딩참고
007js함수> js5-1함수 뒷배경

* 문단 깔끔하게 정렬하는 코드
text-align: justify;
*단어 깨져서 보이게
word-break: break-all;


* 스크롤 부드럽게 넘어가기
html {
  font-size: 10px;
   /* 1rem = 10px */

   /* 스크롤 위치 이동시 부드럽게 애니메이션 이동설정 
   -> 이 설정은 전체 페이지이동일 경우 html에 셋팅함 */
   scroll-behavior: smooth;
} 


mediaQuery참고용
05css학습-05미디어커리-미디어03번 // 비율유지 상자


책넘기기효과
1.
https://web.dev/case-studies/20things-pageflip?hl=ko
2.
https://fliphtml5.com/learning-center/ko/how-to-create-an-interactive-pdf-magazine/
3.
https://www.php.cn/ko/faq/495739.html

    /* 글자배치 세로방향 left-> right */
    writing-mode: vertical-lr;
    letter-spacing: 30px;
    font-size: 24px;
    color: rgb(255, 255, 255);
    text-shadow: 2px 2px 2px #00000070;

********************************

자바 for 문 연습2 참고해서
그림 배열하고 자동만들기
자바 for_in 문
********************************


CSS 실험실-> 햄버거 버튼 三 -> X 버튼
모바일때 사용 버튼

******영상 어둡게 만들기**********
  /* 필터 명도값 조정하여 어두워보이게함 */
    filter: brightness(.5);

/* 마영오*/
 마(진) 영(위) 오(양쪽) 아래 

     /* 가상요소 자신이 block이므로 최소한 
    display를 inline-block로 해야 자신의 크기만큼
    크기를 가지게 된다.*/

****************************************************************
<!--*** 젠코딩***
div*2>ul.link-list>li*5>a[target=_blank] 

****************************************************************
반응형 _컬크로 계산하기

.link-box{
    /* 가로크기는 레이아웃은 아니지만
    12그리드 크기를 기준해서 준다. */
    width: calc(100%/12*10);
}
****************************************************************
<선 가상요소 넣기>

(1) 선 위치 조절방법 ---> 선이 글자랑 붙어나옴
.sub-part-title::before{
    content: '';
    display: block;
    margin: 0 auto;
    max-width: 865px;
    height: 1px;
    background-color: ccc;
}

(2) 선 위치 조절방법 
.sub-part-title {
  /* 박스설정*/
  padding-bottom: 28px;
  text-align: center;
  /* 글자설정*/
  font-size: 36px;
}

/* 구분선은 타이틀 박스의 가상요소로 구성 */
.sub-part-title::before {
  content: "";
  display: block;
  /* 마(진) 영(위) 오(양쪽) 아래 */
  margin: 0 auto 28px;
  max-width: 865px;
  height: 1px;
  background-color: #ccc;
}
****************************************************************

<글 2문단으로 나누기>

     /* 라벨디자인-버튼형 */
      .btn-action {
        /* 위 아래 마진 보더 등 적용위해 */
        display: inline-block;
        margin-top: 50px;
        padding: 20px;
        border: 1px solid rgb(255, 255, 255);
        border-radius: 10px;
        background-color: #ccc;
        /* 손가락모양 */
        cursor: pointer;
      }
      /* 가상요소로 버튼글씨 넣기 */
      .btn-action::before {
        content: "클릭";
      }
      /* 체크박스 체크시 버튼형 라벨 변경하기 */
      #chkbx:checked + .btn-action {
        background-color: rgb(255, 176, 242);
      }
      #chkbx:checked + .btn-action::before {
        content: "따딴";
      }
      /* 체크박스 숨기기 */
      #chkbx {
        display: none;
      }
 #chkbx:checked ~ .move-box #chi{
    /* 투명도 op- opacity:0~1사이값(0투명-반투명-1불투명) */
   left: 100%;
    /* 트랜지션 trs : 3.9초 후 0.1초간 애니 */
    transition: 10s ;}

        #chi {
        position: absolute;
        left: 0%;
        width: 2%;}
